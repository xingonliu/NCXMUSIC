# Prompt 34：Phase 5 收口门禁

执行通用协议，只审计 Agent Alpha。

核对 Prompt 29～33、Agent Runtime、IPC、权限矩阵和功能清单 LLM/AGT/TOL/SEC。复跑三 Provider、单 Turn、打断、限额、搜索播放、真实回执、实体消歧、审批零执行、SelectionCard、写操作、Capability Gateway、重载和故障 E2E。

确认模型看不到 Cookie、Secret、安全等级和未注册能力；无内部 SSE；无 Agent 侧边栏；支付能力无法被发现或调用；副作用不透明重试为零。

生成 `34-phase5-gate.md` Checkpoint。达到 `Agent Alpha` 才解锁 Prompt 35。
