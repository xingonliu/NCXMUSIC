# NcxMusic 产品需求与架构基线

> 文档状态：Draft 0.2（需求访谈完成，P0/P1/P2 决策已冻结）  
> 建立日期：2026-08-03  
> 文档用途：作为产品、设计、架构、开发和验收的唯一需求基线  
> 规则：明确区分“已确认”“暂定方案”“待确认”，未经确认的内容不得直接当作开发结论

## 1. 产品概述

### 1.1 一句话定义

NcxMusic 是一款以网易云音乐数据为基础，具备长期用户画像、自然语言和语音操控、Dynamic Skill/MCP 扩展能力，并由确定性权限系统保护的跨平台 Agent 原生音乐客户端。

### 1.2 产品愿景

用户不再需要逐层寻找歌曲、歌单和功能入口，而是可以直接表达目的：

- “播放一些适合今晚写代码的歌。”
- “把当前歌曲加入我的跑步歌单。”
- “分析一下我最近的音乐口味变化。”
- “找出我收藏过但一年没有播放的歌曲。”
- “执行签到，并告诉我结果。”

Agent 不只是回答问题，而是理解上下文、选择工具、执行操作、反馈结果，并在长期使用中形成稳定的用户记忆。

### 1.3 核心产品原则

1. Agent 是一级交互入口，不是播放器上的附属聊天框。
2. 模型负责理解与决策，权限由确定性代码控制。
3. 所有外部能力必须经过统一工具执行入口。
4. 模型不得猜测歌曲、歌单、歌手或用户 ID。
5. 原始 API 字段不得直接扩散到 UI、画像和 Agent 层。
6. 用户数据、本地记忆和安全设置默认按账户隔离。
7. 动态扩展必须可控、可停用、可追踪、可撤销。
8. 每个 API 必须先测试和分析字段，再进入业务开发。

## 2. 当前已确认的产品决策

| 编号 | 决策 |
| --- | --- |
| C-001 | Agent 主循环纯手写，不使用 Agent 框架。 |
| C-002 | MCP 使用官方 Model Context Protocol TypeScript SDK。具体包版本在技术选型阶段锁定。 |
| C-003 | Skill 采用 Dynamic Skill 机制，从用户本地应用数据目录动态加载。 |
| C-004 | Skill 通过 `SKILL.md` YAML Frontmatter 描述元数据，并可将 Prompt 规则动态注入系统提示词。 |
| C-005 | Skill 中的 JavaScript 可注册为 Agent Tool，但最终执行必须经过统一权限中间件。 |
| C-006 | Windows Shell 默认使用 `powershell.exe -NoProfile -ExecutionPolicy Bypass`。 |
| C-007 | macOS 默认使用 `/bin/zsh`；Linux 优先使用 `/bin/zsh`，不可用时再考虑系统 `$SHELL`。 |
| C-008 | 音乐操作权限和命令执行权限使用两个相互独立的前端控制按钮。 |
| C-009 | 权限判断必须由纯函数和 Node.js 中间件完成，模型不参与权限判断。 |
| C-010 | 需要审批时，底层操作挂起；拒绝时底层逻辑零执行，并返回结构化工具结果。 |
| C-011 | 前端需要“工具执行展示卡片”和“批准/拒绝审批卡片”两类组件。 |
| C-012 | 用户画像和基础记忆按网易云用户 ID 持久化隔离。 |
| C-013 | Agent 页面首次进入时分析用户歌单、喜欢歌曲和播放排行，并形成基础画像。 |
| C-014 | Agent 侧边栏默认打开；用户关闭后持久化该偏好，以后不再自动打开。 |
| C-015 | 支持通过 `Alt+Space` 唤起语音 Agent 小组件。 |
| C-016 | Windows 与 macOS 使用一致的类 macOS 视觉语言。 |
| C-017 | 全屏模式隐藏关闭/缩小等窗口按钮，悬停导航区域后通过渐变模糊遮罩显示。 |
| C-018 | 网易云音乐 API 原则上都应能被封装或适配为 Agent Tool。 |
| C-019 | 冷门接口可通过通用 API Gateway Tool 兜底，但常用业务使用专用组合工具。 |
| C-020 | NcxMusic 作为开源项目开发和发布。 |
| C-021 | 产品定位为高颜值、具备完整 AI Agent 能力的独立音乐播放器。首版不以复刻网易云全部页面为目标。 |
| C-022 | 网易云能力基于 NeteaseCloudMusicApiEnhanced 项目接入。 |
| C-023 | 登录时打开隔离的网易云官方网页登录窗口，不在 NcxMusic 内收集用户的账号密码。 |
| C-024 | 登录成功后由主进程拦截并保存 `MUSIC_U`，在 Node.js API Adapter 请求中以 `MUSIC_U=<value>` 形式携带。 |
| C-025 | NeteaseCloudMusicApiEnhanced 作为应用内本地 Node.js 依赖运行，不依赖用户自行部署的远程 API 服务。具体承载进程待技术验证。 |
| C-026 | 首版包含发现/首页、搜索、歌单详情、专辑详情、歌手详情、歌曲播放详情、歌词、Agent、个人信息和我喜欢的音乐等基础体验。 |
| C-027 | Agent 暂定产品名为“小 N”，正式名称与人格设定后续确认。 |
| C-028 | 首次进入应用提供分步引导，展示品牌、产品能力、Agent 语音操作演示和模型配置。 |
| C-029 | 模型配置支持厂商预设和自定义 OpenAI 兼容地址；用户自行提供 API Key。模型配置允许跳过。 |
| C-030 | 未配置模型时进入 Agent 页面，展示明确空状态和前往模型设置的按钮，不进入不可结束的加载态。 |
| C-031 | 侧栏参考 Codex，拆分为稳定的主导航和用于快速播放的次导航。 |
| C-032 | 首次引导中的网易云登录位于模型配置之前，并允许跳过。 |
| C-033 | 跳过网易云登录后，应用在本地调用 `/register/anonimous` 建立游客会话。 |
| C-034 | 游客状态下个人信息不可用；侧栏底部展示默认头像和“游客”。 |
| C-035 | 侧栏冻结为上中下三区：主导航、歌单次导航、底部账户与设置；底部账户和设置各占一行。 |
| C-036 | 歌单次导航包含我喜欢、自建歌单和收藏歌单；自建默认展开、收藏默认折叠，每组最多展示最近或置顶的 5 个。 |
| C-037 | 首版模型协议冻结为 OpenAI Chat Completions Compatible、Anthropic Messages、Google Gemini `generateContent`。 |
| C-038 | 游客状态完全隐藏歌单次导航，只保留主导航和底部游客/设置区域。 |
| C-039 | 页面采用 Section 化组合结构，每一块内容是可独立开发、加载、失败和刷新复用的组件。 |
| C-040 | 登录用户完成模型配置且用户画像可用后，首页展示“小 N 为你推荐”Section。 |
| C-041 | 小 N 只提供一个连续会话，不设计多会话列表；跨时间使用依靠长期记忆延续。 |
| C-042 | 用户超过 10 分钟未发送消息时结束当前会话块；每个会话块独立存入 SQLite 并生成摘要。 |
| C-043 | 会话块摘要需要支持向量化检索；系统提示词只装配当前块和过去被判定有价值的摘要。 |
| C-044 | 使用独立 Working Memory 文件保存当前块和本轮选中的长期记忆，详细历史按需查询 SQLite。 |
| C-045 | 小 N 触发播放、切歌或队列变化时，显示轻量消息提示，明确操作来自小 N。 |
| C-046 | 前端建设完整的通用组件体系，覆盖基础控件、反馈、浮层、布局和音乐业务组件。 |
| C-047 | Windows 与 macOS 双平台正式首发，Linux 以预览形式提供，不承诺支持等级。 |
| C-048 | 一次 Agent 任务最多 8 轮工具调用，单轮 60 秒，整体 10 分钟；超限时暂停并向用户说明。 |
| C-049 | 工具失败时只读操作自动重试 1 次；写操作禁止自动重试，失败直接如实告知用户。 |
| C-050 | 同名消歧：候选集打分后高置信度直接执行，低置信度弹候选卡片让用户选择，禁止静默猜测。 |
| C-051 | 音乐 M3 自动允许收藏/取消收藏、创建/删除歌单、歌单内添加/移除歌曲；评论、签到等社交写入仍需审批。 |
| C-052 | 音乐 M4 默认关闭；开启后仅头像、昵称等展示资料自动执行，手机、邮箱等认证资料任何等级都需审批。 |
| C-053 | Shell 权限采用命令白名单 + 危险参数模式 + 工作目录边界（默认应用数据目录下专用工作区），纯函数判定，LLM 不参与。 |
| C-054 | 审批支持“仅本次 / 本会话 / 总是允许”三档；持久化豁免写入用户可管理、可撤销的策略表。 |
| C-055 | 审批卡片关闭视为拒绝；超时（默认 5 分钟，可配置）视为拒绝；应用退出后任务保留，恢复时重新校验。 |
| C-056 | 设置硬禁清单（如访问 Credential Vault、修改权限策略、执行未声明工作目录的命令），任何等级与用户策略不可覆盖。 |
| C-057 | 画像允许完整性格分析；任何结论必须附证据摘要与不确定性说明，措辞限定为音乐偏好倾向分析而非诊断。 |
| C-058 | 画像权重由产品固定默认值，设置页提供高级调整。 |
| C-059 | 画像增量更新：启动时检查；喜欢的音乐与自建歌单的加权累计变化超过阈值（30 首；喜欢的音乐每首权重 1.5，自建歌单每首权重 1）触发。 |
| C-060 | 用户可查看画像详情、纠正单条偏好、删除全部画像、暂停更新。 |
| C-061 | 画像、对话等业务数据默认明文存储，设置页可开启本地加密。 |
| C-062 | 模型分析允许发送完整歌单；发送前在初始化卡片告知数据范围与费用影响，设置页提供“仅发送聚合结果”开关。 |
| C-063 | ASR 本地优先（系统语音识别/本地 Whisper），云 ASR 作为可选 Provider，费用由用户自带 Key 承担。 |
| C-064 | 语音松手后浮窗直接关闭，执行完成后通过 Windows/macOS 原生通知 API 推送结果摘要。 |
| C-065 | 首版不做 TTS 播报。 |
| C-066 | 语音快捷键允许用户修改。 |
| C-067 | 语音原始音频不落盘，流式处理即弃。 |
| C-068 | Skill 来源为用户手写 + 本地导入；Git 仓库安装与应用内市场列入后续版本。 |
| C-069 | Skill 安装时按 SKILL.md 声明一次性同意权限清单；每次实际执行仍经过统一权限中间件与硬禁清单。 |
| C-070 | Skill 支持纯 JS 依赖包，安装前展示依赖清单并锁定版本；原生模块依赖暂不支持。 |
| C-071 | 新增 `mcp_manager` Agent Tool，允许小 N 自主安装/删除 MCP Server；属高风险操作必须审批。 |
| C-072 | MCP 传输支持 stdio 与 Streamable HTTP；凭据存 Credential Vault；配置支持可视化编辑与 .mcp.json 导入。 |
| C-073 | Skill/MCP 工具与内置工具重名时内置优先，设置页展示冲突清单并允许重命名外部工具。 |
| C-074 | 首批 API 清单按首版页面反推：登录/用户、搜索、歌单 CRUD、歌曲详情/URL/歌词、专辑、歌手、推荐、评论、签到、播放排行。 |
| C-075 | 为每个 API 创建专用测试 JS，多次调用覆盖不同参数与边界分支，确定返回数据格式。 |
| C-076 | 字段合并规则：权威（账户级优先于公共）+ 新鲜度 + 完整度组合，配置化可调。 |
| C-077 | 缓存分级：用户数据 24h + 启动后台刷新；首页推荐 30min；搜索建议 1h；离线展示缓存并标注；Section 级降级；首页顶部菜单栏提供手动刷新按钮。 |
| C-078 | 网易云 Cookie、Token 和设备信息全部存入 Credential Vault，由 API Adapter 注入，不落明文。 |
| C-079 | 视觉基调为 macOS 原生质感，浅色优先，参考 WWDC 2026 设计语言。 |
| C-080 | 小 N 侧边栏默认 320px，可调 260~480px，可收起为窄条。 |
| C-081 | 工具卡片展示状态、参数摘要、耗时、结果摘要，支持展开技术详情。 |
| C-082 | 保留 Windows 原生窗口能力，Snap Layout 跟随系统。 |
| C-083 | 全屏（隐藏窗口控件）与沉浸（播放详情大歌词）为两个独立状态，最大化保留系统行为。 |
| C-084 | 中文首版；无障碍基础（语义树、键盘完整操作、对比度达标）；多语言架构预留，首版不做 i18n。 |

## 3. 目标用户与使用场景

> 本节尚未完成用户访谈，以下用户范围仍为暂定假设；产品形态和开源属性已经确认。

### 3.1 暂定目标用户

- 网易云音乐重度用户，拥有多年歌单和收藏数据。
- 希望通过自然语言降低找歌、整理歌单和操作播放器成本的用户。
- 经常在电脑上学习、工作、创作，需要场景化背景音乐的用户。
- 愿意配置模型、Skill、MCP 或本地自动化能力的进阶用户。
- 偏好高完成度、强视觉表现和桌面原生体验的开源软件用户。

### 3.2 核心场景

1. 自然语言搜索并播放歌曲。
2. 根据长期画像生成符合当前场景的推荐。
3. 对当前歌曲、队列和歌单进行上下文操作。
4. 分析用户口味、性格倾向和近期变化。
5. 通过按住快捷键快速发出语音命令。
6. 使用 MCP、Dynamic Skill 和 Shell 扩展 Agent 能力。

## 4. 核心用户闭环

```text
登录网易云
  → 获取账户与音乐数据
  → 归一化实体和字段
  → 读取或生成用户画像
  → 用户通过文字/语音表达意图
  → 手写 Agent 选择工具
  → 参数与实体解析
  → 确定性权限拦截
  → 自动执行或等待审批
  → 返回结果并更新会话/长期记忆
```

## 5. 功能需求

### 5.1 音乐客户端基础能力

- 账户登录、退出和会话恢复。
- 首页、搜索、歌手、专辑、歌曲、歌单、MV、评论等页面。
- 播放、暂停、上一首、下一首、进度、音量、播放模式。
- 播放队列的增加、移除、清空和重排。
- 收藏/取消收藏歌曲。
- 创建、删除和管理歌单。
- 添加歌曲到歌单、从歌单移除歌曲。
- 评论查看、发表评论及其他互动能力。
- 签到等账户辅助操作。

> 产品不以完整复刻网易云官方客户端为目标，但“高颜值播放器”首版的准确页面与能力边界仍待确认。

#### 首版页面范围

- 发现页：首页内容、推荐、最近播放和快捷入口。
- 搜索页：搜索输入、建议、历史、分类结果和结果操作。
- 歌单详情页：歌单信息、歌曲列表、播放和歌单操作。
- 专辑详情页：专辑信息、歌曲列表、收藏和播放。
- 歌手详情页：歌手信息、热门歌曲、专辑及相似/推荐歌手。
- 播放详情页：当前歌曲、封面、播放控制、歌词和队列入口。
- 独立歌词沉浸态：大歌词、背景视觉和全屏体验。
- 小 N：Agent 会话、工具执行和审批交互。
- 我的音乐：我喜欢的音乐、自建歌单、收藏歌单及后续音乐资产。
- 账户页：网易云账户信息、画像摘要和账户操作。
- 设置页：模型、Agent、MCP、Skill、安全、播放、外观和存储设置。

#### Section 化页面结构

所有聚合页面由独立 Section 组件拼接，不在单个页面组件内直接堆叠接口和业务逻辑。

```text
PageShell
  ├─ PageHeader
  ├─ Section A
  ├─ Section B
  ├─ Section C
  └─ PlayerSafeArea
```

每个 Section 必须独立声明：

- Section ID、标题和展示条件。
- 数据依赖和缓存键。
- Loading、Empty、Error、Ready 状态。
- 刷新、重试和取消能力。
- 最小高度与响应式布局。
- 埋点和错误边界。

一个 Section 请求失败不能阻断整个页面。Section 支持配置排序、按条件隐藏和后续灰度扩展。

首页暂定 Section：

- 继续播放/最近播放。
- 每日或平台推荐。
- 小 N 为你推荐。
- 推荐歌单。
- 推荐歌手。
- 其他通过 API 实测后确认的内容。

“小 N 为你推荐”只在以下条件同时成立时展示：

1. 已登录正式网易云账号。
2. 已完成可用的大模型配置。
3. 当前网易云用户 ID 对应的画像状态为 `ready`。

游客、未配置模型或画像尚不可用时不展示空壳 Section，避免首页出现无意义占位。

#### Codex 式双层侧栏（已确认）

侧栏拆分成两个语义明确的区域：主导航负责切换产品能力，次导航负责让用户尽快听到自己的音乐。

```text
┌─────────────────────┐
│ 主导航（顶部固定）    │
│   发现               │
│   搜索               │
│   小 N               │
├─────────────────────┤
│ 歌单次导航（中部弹性） │
│   我喜欢             │
│   我创建的歌单        │
│     最近/置顶的 5 个   │
│     查看全部          │
│   我收藏的歌单        │
│     最近/置顶的 5 个   │
│     查看全部          │
│                     │
│   内容超长时仅本区滚动  │
├─────────────────────┤
│ 头像 + 用户名（第一行） │
│ 设置按钮（第二行）     │
└─────────────────────┘
```

次导航包含“我喜欢 + 我创建的歌单 + 我收藏的歌单”，使用折叠、置顶和数量限制控制长度，而不是把所有歌单永久铺开：

- “我喜欢”固定第一位，单击直接进入并可立即播放。
- “我创建的歌单”默认展开，优先显示置顶或最近播放的 5 个歌单。
- “我收藏的歌单”默认折叠，展开后同样只显示置顶或最近播放的 5 个歌单。
- 两组歌单均提供“查看全部”，进入完整音乐库页面。
- 用户可以置顶、取消置顶和调整同组歌单顺序。
- 次导航区域独立滚动，不挤压主导航与底部账户区。
- 歌单数据从本地缓存优先恢复，后台增量刷新，避免启动时等待全部接口。

布局约束：

- 主导航高度由内容决定，不参与滚动。
- 底部账户区固定在侧栏底部，不参与滚动。
- 次导航占据两者之间的剩余高度，必须设置最小高度为 0，并只在自身范围内纵向滚动。
- 次导航内容不得覆盖、挤出或推动底部账户区。
- 账户行展示头像和用户名；设置行单独展示设置入口，保持两行结构。

选择三类内容的原因：收藏歌单同样是用户高频播放资产，完全隐藏会增加路径；直接全部展开又会让重度用户侧栏失控。折叠和最近/置顶策略可以同时解决两者。

其他规则：

- “主页”建议命名为“发现”，表达推荐、最近播放和内容探索。
- 个人信息通过侧栏底部账户区进入，不占用主导航。
- 歌单、专辑、歌手和歌曲详情是由内容进入的二级路由。
- “歌手推荐”作为发现页内容模块和二级列表页。
- 歌曲详情与歌词优先合并为“播放详情”的不同展示态。

该信息架构、次导航内容和默认折叠规则已确认。

### 5.2 官方网页登录与 Cookie 会话

#### 已确认登录方向

1. 用户点击登录后，NcxMusic 打开独立、隔离的官方网易云网页登录窗口。
2. 登录页面由网易云提供，NcxMusic 不创建手机号、密码或验证码输入表单。
3. 主进程监听登录 Session 的 Cookie 变化，只在网易云相关域名中查找 `MUSIC_U`。
4. 捕获到 `MUSIC_U` 后，不立即把“拿到 Cookie”视为登录完成。
5. Node.js API Adapter 使用 `cookie: "MUSIC_U=<value>"` 调用 `/login/status` 或 `/user/account`。
6. 只有验证成功并获得稳定网易云用户 ID 后，才进入已登录状态并关闭登录窗口。
7. 本地记忆、画像、缓存和设置随后切换到该网易云用户 ID 对应的账户空间。

#### Credential Vault

`MUSIC_U` 等同于长期登录凭据，必须遵守：

- 只允许 Electron 主进程或独立本地服务读取。
- Renderer、Agent Prompt、模型请求、Tool Call 参数和 SSE 事件均不得包含原值。
- 本地持久化前使用操作系统安全能力加密；不以明文 JSON、LocalStorage 或普通配置文件保存。
- 日志中只允许记录“是否存在、校验状态、过期状态”，禁止记录原值或可逆片段。
- API Adapter 在请求发出前从 Credential Vault 读取，并在内部拼接 `MUSIC_U=<value>`。
- 优先通过 POST body 的 `cookie` 字段传递，避免凭据出现在 URL、代理日志和历史记录中。
- 退出登录时清除 Credential Vault、登录 Session 和 API 会话缓存，但画像是否保留由用户选择。
- 任何本地凭据、调试样本和登录 Session 文件都必须排除在 Git 之外。

#### 登录窗口安全边界

- 登录窗口使用独立 Session Partition，不和主应用页面共享任意 Web 权限。
- 登录窗口关闭 Node Integration，开启 Context Isolation 与 Sandbox。
- 只允许网易云登录所需域名；新窗口、下载、外部协议和非预期导航默认阻止。
- 登录页面不得获得 NcxMusic 的 Agent、Shell、MCP、文件或播放器 IPC 权限。
- 登录超时、用户关闭、Cookie 校验失败和账号切换必须有明确状态。

#### 暂定状态机

```text
logged_out
  → opening_official_login
  → waiting_for_cookie
  → validating_cookie
  ├─ authenticated
  ├─ validation_failed
  └─ cancelled

authenticated
  → session_expired
  → opening_official_login
```

#### 游客会话

用户在首次引导中跳过网易云登录时：

1. 本地 API Adapter 调用 `/register/anonimous` 获取游客 Cookie。
2. 游客 Cookie 进入独立 Credential Vault 槽位，不与 `MUSIC_U` 混用。
3. 应用进入 `guest` 会话，可使用搜索、公开内容和游客凭据允许的播放接口。
4. 歌单次导航完全隐藏，不显示置灰的我喜欢、自建歌单或收藏歌单入口。
5. 账户、我喜欢、创建/收藏歌单、用户画像等登录能力不可用。
6. 侧栏底部账户行显示默认头像和“游客”，不伪造网易云用户 ID；第二行设置按钮仍可用。
7. 用户后续完成官方登录后切换到 `authenticated` 会话，游客凭据停止用于请求，并显示歌单次导航。
8. 用户退出正式账户后，重新建立或恢复游客会话，隐藏歌单次导航，播放器仍可继续使用公共能力。

游客登录接口只能改善部分未登录请求的验证问题，不代表游客拥有正式账户的个性化、收藏和写入权限。每个 API 仍需通过真实请求测试确认游客可用性。

当前要求游客账户行点击无效，但跳过登录后必须保留一个明确的二次登录入口。入口位置暂定为设置页“网易云账户”，是否在游客账户行旁增加“登录”按钮待确认。

#### 文档核验结论

NeteaseCloudMusicApiEnhanced 文档确认：可以手动传入 Cookie；从浏览器获取时只需要 `MUSIC_U`，请求值使用完整的 `MUSIC_U=xxxx` 字符串。GET 需要编码后放入查询参数，POST 可放在 body 的 `cookie` 字段中。NcxMusic 默认采用后者，并通过 `/login/status` 或 `/user/account` 校验登录状态。

### 5.3 手写 Agent Runtime

Agent Runtime 至少包含：

- 会话消息管理。
- 系统提示词组装。
- Dynamic Skill Prompt 按需注入。
- Tool Schema 注册与模型可见工具裁剪。
- 流式模型响应解析。
- Tool Call 参数校验。
- 多轮工具调用循环。
- 工具结果回填。
- 取消、超时、重试和最大步数控制。
- 审批等待与恢复。
- 会话短期上下文。
- 长期记忆读取与写入。
- 运行事件日志与调试信息。

#### 单会话模型

- 产品只有一个持续存在的小 N 会话入口。
- 不提供“新建会话”、会话列表或并行会话切换。
- UI 展示的是当前活跃会话块；历史内容通过时间分隔、搜索和记忆召回使用。
- 关闭应用后恢复当前未结束的会话块和 Working Memory。

#### 会话分块规则

SQLite 是消息和会话块的事实来源。用户最后一次发送消息后超过 10 分钟，再次发送消息时创建新块，并将上一块标记为完成。

每个会话块至少包含：

```text
blockId
neteaseUserId / guestInstallationId
startedAt
lastUserMessageAt
endedAt
messages
toolCallRefs
summary
summaryImportance
embeddingRef
status
```

分块结束后异步生成摘要；摘要失败不影响新块继续聊天，但需要进入重试队列。每个块都必须最终拥有摘要，才能进入长期记忆检索。

#### Context Assembler

模型请求不直接装入全部 SQL 历史，而由 Context Assembler 组装：

```text
System Rules
+ Dynamic Skills
+ User Profile
+ Current Working Memory
+ 当前会话块的必要消息
+ 检索命中的高价值历史摘要
+ 当前播放器/页面上下文
```

Working Memory 文件是可重建的 Prompt 快照，不是历史事实来源。建议按网易云用户 ID 隔离，保存：

- 当前 block ID。
- 当前块仍需进入上下文的消息引用或裁剪内容。
- 本轮选中的长期摘要 ID。
- 用户当前目标、未完成事项和重要临时事实。
- 上次成功写入时间与 Schema 版本。

Working Memory 采用原子替换写入；损坏或版本不兼容时从 SQLite 重建。文件不保存 API Key、Cookie 或 Shell 敏感输出。

#### 长期记忆检索

- 每个完成的会话块生成结构化摘要和检索文本。
- 摘要标注人物、偏好、承诺、任务、音乐实体、时间和重要性。
- 先用账户过滤和时间/实体条件缩小范围，再进行向量相似度检索。
- 召回结果还需经过相关性、重要性、新鲜度和冲突检查，不能只凭向量距离进入 Prompt。
- Agent 可在需要时调用 `memory_search` Tool 查询更详细的块或原始消息。
- 原始消息默认不直接进入长期 Prompt，只有明确需要细节时读取。

Embedding 的来源、本地模型体积、重新索引规则和无 Embedding 服务时的降级方案待确认。

### 5.4 Agent Tool 分层

#### 高级业务工具

- `smart_search_and_play`
- `playlist_manager`
- `get_music_comments`
- `artist_explorer`
- `user_profile_memory`
- 播放器状态和队列管理工具
- 签到与账户状态工具

#### 原子音乐工具

- 搜索歌曲/歌手/专辑/歌单。
- 获取歌曲详情、URL、歌词。
- 获取/修改歌单。
- 收藏/取消收藏。
- 获取/发布评论。
- 获取当前播放状态。

#### 通用 API Gateway

仅在没有专用工具时使用。必须具备：

- API 路径白名单或能力清单。
- 参数 Schema 校验。
- 登录态和账户权限校验。
- 风险动作分类。
- 响应字段归一化或安全裁剪。
- 限流、超时、取消和审计日志。

禁止模型通过 Gateway 绕过专用工具的安全策略。

### 5.5 实体解析与上下文池

模型不得猜测业务 ID。系统维护统一实体引用：

```text
当前歌曲       → songId
当前歌手       → artistId
当前专辑       → albumId
当前歌单       → playlistId
最近搜索候选   → entity IDs
最近工具结果   → entity references
```

工具参数应优先支持语义引用：

```json
{
  "songRef": "current",
  "playlistRef": "我的跑步歌单"
}
```

执行前由 Entity Resolver 解析为稳定 ID；已确认的消歧策略：对候选集打分（流行度、播放历史、画像偏好），高置信度直接执行，低置信度弹出候选卡片让用户选择，禁止静默猜测。

### 5.6 Dynamic Skill

暂定目录：

```text
app.getPath("userData")/skills/<skill_name>/
├── SKILL.md
├── index.js
└── assets/
```

加载步骤：

1. 扫描 Skill 目录。
2. 解析 YAML Frontmatter 和正文规则。
3. 校验名称、版本、入口和权限声明。
4. 建立 Skill 索引。
5. 根据任务按需注入 Prompt，避免全部加载到上下文。
6. 在隔离环境加载 JavaScript 工具。
7. 将工具注册到统一 Tool Registry。
8. 所有调用仍经过 Tool Executor 与权限中间件。

需要支持启用、禁用、错误隔离、版本识别和刷新。

已确认：

- 来源：用户手写 + 本地导入；Git 仓库安装与应用内市场列入后续版本。
- 授权：安装时按 SKILL.md 声明一次性同意权限清单；每次实际执行仍经过统一权限中间件与硬禁清单。
- 依赖：支持纯 JS 依赖包，安装前展示依赖清单并锁定版本；原生模块依赖暂不支持。
- 命名冲突：与内置工具重名时内置优先，设置页展示冲突清单并允许重命名外部工具。

待确认：Skill 来源信任、签名、更新与卸载规则。

### 5.7 MCP

- NcxMusic 作为 MCP Client 连接外部 Server。
- 将 MCP Server 暴露的工具映射到统一 Tool Registry。
- MCP 工具不能绕过 NcxMusic 的安全分级与审批流程。
- 需要管理 Server 配置、连接状态、工具列表、启停和错误信息。
- 已确认：传输支持 stdio 与 Streamable HTTP；凭据存 Credential Vault；配置支持可视化编辑与 .mcp.json 导入。
- 已确认：提供 `mcp_manager` Agent Tool，允许小 N 自主安装/删除 MCP Server；该操作属高风险，必须经过审批；安装时展示 Server 能力声明。

### 5.8 用户画像与长期记忆

画像输入包括：

- 用户创建的全部歌单。
- 用户收藏的歌单，权重低于自建歌单。
- 喜欢的音乐。
- 歌曲加入歌单或收藏的时间。
- 最近更新时间，越新权重越高。
- 播放排行，头部歌曲权重最高。
- 后续可扩展的搜索、跳过、单曲循环和主动删除等行为信号。

画像至少输出：

- 偏好歌手、曲风、语言、年代和情绪。
- 稳定偏好与近期偏好的差异。
- 使用场景倾向。
- 推荐解释所需的证据摘要。
- 可供 Agent 使用的简洁画像 Prompt。

基础信息与画像按网易云用户 ID 分目录或分库保存。已确认：业务数据默认明文存储，设置页可开启本地加密；模型分析时可发送完整歌单，但发送前须在初始化卡片告知数据范围与费用影响，设置页提供“仅发送聚合结果”开关。数据格式、保留周期仍待确认。

#### 用户画像生命周期（已确认）

画像状态：

```text
unavailable   // 游客或缺少前置条件
collecting    // 获取并归一化歌单、喜欢与播放排行
ready_local   // 本地特征已完成，等待模型分析
analyzing     // 模型正在形成画像
ready         // 可用于推荐和 Agent Prompt
stale         // 已有画像可用，但需要后台更新
failed        // 初始化或更新失败
```

产品经理建议采用“静默采集 + 明示模型分析”的混合方式：

1. 登录成功后静默获取并归一化基础账户、歌单、喜欢和播放排行，不调用大模型。
2. 若本地存在同一用户的旧画像，立即加载并在后台判断是否需要增量更新。
3. 首次需要调用用户自备模型分析时，在小 N 页面展示一次“让小 N 了解你的音乐口味”初始化卡片，说明会发送的摘要数据和可能产生的 Token 费用。
4. 用户确认后执行模型分析；离开页面不取消后台任务。
5. 分析期间小 N 的普通聊天和基础音乐控制仍可使用，不使用全页阻塞加载。
6. 完成后首页自动出现“小 N 为你推荐”Section。
7. 失败时保留已收集数据，提供重试，不重新请求所有网易云接口。
8. 增量更新触发：启动时检查；喜欢的音乐与自建歌单的加权累计变化（喜欢的音乐每首权重 1.5，自建歌单每首权重 1）超过 30 时触发。
9. 用户可查看画像详情、纠正单条偏好、删除全部画像或暂停更新。

如果首次引导已明确获得用户对画像分析的同意，则登录和模型配置同时完成后可以自动开始，无需在小 N 页面再次点击。

### 5.9 双权限系统

#### 音乐操作权限

| 等级 | 当前理解 |
| --- | --- |
| M1 | 所有 AI 代操作均需审批。 |
| M2 | 播放、暂停、上下首和播放队列变更无需审批；其他操作审批。 |
| M3 | 已确认：收藏/取消收藏、创建/删除歌单、歌单内添加/移除歌曲自动执行；发布/删除评论、签到、关注等社交与账户写入仍需审批。 |
| M4 | 已确认：默认关闭；开启后仅头像、昵称等展示资料自动执行，手机、邮箱等认证资料任何等级都需审批。 |

#### 命令执行权限

| 等级 | 已确认方案 |
| --- | --- |
| S1 | 所有 Shell 命令均需审批。 |
| S2 | 明确判定为只读的命令自动执行：命令白名单 + 危险参数模式，纯函数判定。 |
| S3 | 工作区（默认应用数据目录下专用工作区）内常规读写命令自动执行，外部或高风险命令审批。 |
| S4 | 大多数命令自动执行，但仍受产品级不可绕过规则与硬禁清单约束。 |

#### 判断与审批流程

```text
Tool Call
  → 参数 Schema 校验
  → classifyOperation(tool, action, args)
  → evaluatePolicy(operation, musicLevel, shellLevel)
  ├─ allow: 执行底层 handler
  ├─ ask: 创建审批请求并挂起
  └─ deny: 不执行并返回结构化拒绝
```

审批请求必须包含操作名称、影响对象、关键参数、风险解释和可选过期时间。审批事件当前要求通过 SSE 推送；Electron 本地进程拓扑和 SSE/IPC 的职责边界待确认。

已确认的审批行为：

- 审批支持“仅本次允许”“本会话允许”“以后总是允许”三档；持久化豁免写入用户可管理、可撤销的策略表。
- 关闭审批卡片视为拒绝；超时（默认 5 分钟，可配置）视为拒绝；应用退出后任务保留，恢复时重新校验状态，必要时重新发起审批。
- 设置硬禁清单（如访问 Credential Vault、修改权限策略、执行未声明工作目录的命令），任何安全等级与用户策略都不可覆盖。

### 5.10 工具执行与审批组件

工具展示卡片至少包含：

- 工具名称及类别。
- 当前状态：排队、运行、成功、失败、已取消。
- 对用户可理解的参数摘要。
- 执行耗时和结果摘要。
- 可展开的技术详情。

审批卡片至少包含：

- Agent 想做什么。
- 将影响哪些歌曲、歌单、账户数据或文件。
- 风险等级和原因。
- 批准、拒绝操作（三档：仅本次 / 本会话 / 总是允许）。
- 防重复提交；关闭视为拒绝；超时默认 5 分钟可配置；窗口关闭后任务保留待恢复。

#### 播放操作提示与事件日志

播放器操作采用“结构化事件日志 + 轻量用户提示”两层设计，两者用途不同：

- Action Journal 供小 N、调试和状态协调查询。
- Toast/Message 供用户即时感知操作来源。

事件至少包含：

```text
eventId
actor          // user | agent | system
action         // play | pause | next | previous | queue_add | queue_remove | queue_replace ...
targetRefs
beforeRevision
afterRevision
createdAt
result
toolCallId
```

规则：

- 用户直接播放控制始终即时生效，不等待 Agent。
- 小 N 执行音乐操作时读取播放器最新状态，不使用规划阶段的过期队列快照。
- 用户与小 N 在接近时间操作时不弹冲突模态框，以最终播放器状态为准。
- 小 N 的成功操作显示轻量提示，例如“小 N 为你播放了《晴天》”。
- 大范围队列变更提示影响数量，并在底层操作可逆时提供“撤销”。
- 小 N 可通过 `get_recent_player_actions` 查询最近事件，理解用户是否刚刚手动切歌或调整队列。
- 高频播放进度不写入 Action Journal；只记录具有语义的状态变化。
- 日志保留数量和时间待确认，避免无限增长。

### 5.11 Shell Tool

- Windows 固定启动 PowerShell，并使用 `-NoProfile -ExecutionPolicy Bypass`。
- macOS 使用 `/bin/zsh`。
- Linux 优先 `/bin/zsh`，回退策略待确认。
- Shell Tool 必须声明工作目录、超时、环境变量策略和输出上限。
- 命令执行必须支持取消、标准输出/错误流式返回和退出码。
- 敏感信息脱敏、环境变量访问、路径范围及危险命令策略待确认。
- 已确认：只读判定采用命令白名单 + 危险参数模式，工作目录默认应用数据目录下专用工作区，全部由纯函数判定，LLM 不参与权限判断。

### 5.12 语音快捷操作

当前目标交互：

```text
按住 Alt+Space
  → 显示声波浮窗
  → 聆听中
松开
  → 结束录音
  → 浮窗直接关闭
  → 识别并交给 Agent
  → 执行完成后通过系统原生通知 API 推送结果摘要
```

浮窗根据 `screen.getPrimaryDisplay().workArea` 定位在工作区底部附近，避开 macOS Dock 与 Windows 任务栏。

已确认事项：

- 松手后浮窗直接关闭，执行完成后通过 Windows/macOS 原生通知 API 推送结果摘要。
- 首版不做 TTS 播报，不提供语音回复。
- ASR 本地优先（系统语音识别/本地 Whisper），云 ASR 作为可选 Provider，费用由用户自带 Key 承担。
- 快捷键允许用户修改。
- 语音音频不落盘，流式处理即弃。

待确认事项：

- `Alt+Space` 与 Windows 系统/PowerToys 快捷键冲突的处理。
- 实现全局按键抬起监听所需的原生能力与 macOS 权限提示。

### 5.13 窗口与视觉体验

- Windows 与 macOS 采用统一类 macOS 风格；视觉基调为 macOS 原生质感，浅色优先，参考 WWDC 2026 设计语言。
- 全屏/沉浸模式隐藏系统窗口操作按钮。
- 导航区域 Hover 后显示关闭、缩小等控件。
- 控件背景采用渐变模糊遮罩。
- 处理 Windows DWM、macOS vibrancy、缩放比例和多显示器差异。
- 自定义标题栏必须保留可拖动区域、双击行为和窗口可访问性。

已确认：全屏（隐藏窗口控件）与沉浸（播放详情大歌词）为两个独立状态，最大化保留系统行为；无障碍首版目标为语义树、键盘完整操作与对比度达标；中文首版，多语言架构预留。视觉规范、主题、最小窗口尺寸、Linux 支持等级仍待确认。

### 5.14 首次引导与模型配置

#### 首次引导目标

首次引导不是功能说明书，而是让用户在较短时间内理解三件事：NcxMusic 是什么、小 N 能做什么、如何让小 N 开始工作。

#### 暂定引导流程

```text
01 欢迎
   Logo + NcxMusic + 一句话价值主张

02 认识播放器
   高颜值播放、歌词、歌单与个性化推荐的视觉演示

03 认识小 N
   动画演示：唤醒浮窗 → 说出“下一首” → 小 N 调用工具 → 完成切歌

04 网易云账户
   打开官方网页登录；允许跳过，跳过后自动进入游客模式

05 数据与安全
   简述本地记忆、音乐/命令双权限和 API Key 的本地安全存储

06 配置模型
   选择厂商预设或自定义服务 → 填写 API Key → 拉取模型 → 能力测试

07 完成
   进入应用；也可以跳过模型配置后进入
```

动效资产优先使用 WebM、Lottie 或程序动画，不把大体积 GIF 作为主要方案。引导页支持返回、下一步、跳过和以后重新打开。

#### 未配置模型的行为

- 音乐播放器功能仍可正常使用。
- 小 N 导航入口保持可见，不伪装成不可用或隐藏功能。
- 进入小 N 后展示未配置空状态、用途说明和“前往模型设置”。
- 需要 Agent 的语音命令给出相同引导，不静默失败。
- 用户配置并验证成功后，空状态自动切换为正常会话。

#### Provider Profile

每个模型服务保存为一个独立 Profile：

```text
providerId
displayName
protocol              // 首版为 openai-chat-completions
baseUrl
modelsPath
chatCompletionsPath
credentialRef         // 只引用 Credential Vault，不保存明文 Key
modelId
customHeaders
capabilitySnapshot
lastVerifiedAt
```

首版提供常见厂商预设。预设仅填写厂商名、官方地址、接口路径、图标和兼容规则，不内置用户密钥。

自定义服务允许填写中转站或兼容服务的名称、Base URL、API Key、模型 ID和必要的自定义 Header。API Key 不进入普通设置文件、Renderer 持久化、日志、Prompt 或远程配置服务。

#### 模型列表不能只靠写死

不存在一个能够可靠覆盖所有官方厂商、中转站和本地服务的统一模型目录。采用四层策略：

1. **内置最小预设**：随版本提供稳定厂商入口和接口规则，保证离线时能填写。
2. **远程 Provider Manifest**：从 NcxMusic 官方 GitHub 仓库读取有版本号和签名/校验值的静态清单，用于更新厂商地址、弃用信息和能力提示；该服务永远不接收用户 API Key。
3. **实时模型枚举**：用户填写凭据后，优先调用厂商/中转站的 Models Endpoint，获得当前账号真正可用的模型 ID。
4. **手动模型 ID**：服务不支持模型枚举、返回失败或中转站做了裁剪时，用户始终可以手动填写。

OpenAI 标准接口通常通过 `GET /v1/models` 返回当前凭据可用的模型；DeepSeek 官方当前提供 `GET /models`。具体路径由 Provider Preset 决定，不通过盲目向第三方域名尝试多个地址来探测。

模型列表通常只提供模型 ID、所有者等基础信息，不能可靠证明是否支持 Tool Call、流式输出或特定参数。因此保存前执行分层测试：

1. 连接与鉴权测试。
2. 模型列表或指定模型可访问测试。
3. 最小流式对话测试。
4. 无副作用的最小 Tool Call 测试。

只有通过 Tool Call 测试的模型才能标记为“小 N 已验证”。失败时允许保存配置，但必须显示“不支持 Agent 工具或尚未验证”，避免用户配置了只能聊天、不能控制播放器的模型。

#### 首版协议范围（已确认）

为覆盖主流用户同时控制维护成本，首版只提供三个一等协议适配器：

| 协议适配器 | 用途 | 首版结论 |
| --- | --- | --- |
| OpenAI Chat Completions Compatible | OpenAI、大多数国内厂商、中转站、OpenRouter、兼容的本地服务 | 首版支持，作为默认兼容层 |
| Anthropic Messages | Claude 官方 API，原生 content blocks、tool use/tool result 和 SSE 事件 | 首版支持 |
| Google Gemini `generateContent` | Gemini 官方 API，原生 parts、functionCall/functionResponse、流式与后续多模态能力 | 首版支持 |

厂商策略：

- DeepSeek、通义千问、Kimi、智谱等优先使用其 OpenAI 兼容入口，不为每个厂商单独维护 Adapter。
- 中转站默认使用 OpenAI 兼容 Adapter，并允许自定义 Base URL、路径和 Header。
- Ollama、LM Studio 等本地服务首版优先走 OpenAI 兼容模式，不维护专属协议。
- AWS Bedrock、Azure AI、Vertex AI、Cohere、Mistral 等原生协议暂不进入首版；有真实用户需求后再评估。
- OpenAI Responses API、Gemini Live API 等增强协议作为后续能力，不阻塞基础 Agent 上线。

所有 Provider Adapter 最终转换成统一内部事件：文本增量、思考增量、工具调用开始/参数增量/完成、用量、结束原因和错误。这样 Agent 主循环与权限系统不需要理解厂商差异。

#### 设置页模型管理

- 支持新增、编辑、复制、删除和启停多个 Provider Profile。
- 指定当前 Agent 使用的默认 Profile 与模型。
- 展示连接状态、最后验证时间、延迟和能力标签。
- 支持刷新模型列表和手动输入模型 ID。
- 修改 Base URL 或 API Key 后自动将旧能力测试标记为失效。
- 删除正在使用的 Profile 前要求用户选择新的默认配置或确认停用 Agent。

### 5.15 前端通用组件体系

前端不得在业务页面重复实现按钮、弹窗、消息和列表交互。组件体系分为四层：

#### Design Tokens

- 颜色、字体、字号、行高。
- 间距、圆角、阴影、模糊和描边。
- 动效时长、缓动、层级和透明度。
- 深色/浅色及平台差异变量。
- Focus、Hover、Pressed、Disabled、Danger 等状态。

#### 基础组件

- Button、IconButton、ButtonGroup。
- Input、Textarea、SearchInput、Select、Combobox。
- Checkbox、Radio、Switch、Slider。
- Avatar、Badge、Tag、Tooltip、Separator。
- Card、Tabs、Accordion、ScrollArea、Skeleton。
- Progress、Spinner、EmptyState、ErrorState。

#### 浮层与反馈组件

- Message/Toast：短时轻量反馈，支持成功、警告、错误和 Agent 来源样式。
- Notification：可保留、信息量更大的通知。
- Dialog/Modal：需要用户集中处理的任务。
- ConfirmDialog：危险操作确认。
- Drawer/Sheet：队列、详情、设置等侧向内容。
- Popover、DropdownMenu、ContextMenu。
- CommandPalette：后续统一快捷操作入口。

浮层统一管理 Z-Index、焦点锁定、Escape、点击外部关闭、键盘导航、动画和屏幕阅读器语义。

#### 布局与音乐业务组件

- AppShell、SidebarLayout、PageShell、Section、SectionHeader。
- ResponsiveGrid、VirtualList、SplitPane。
- TrackRow、SongList、AlbumCard、ArtistCard、PlaylistCard。
- PlayerBar、QueueDrawer、LyricView、MediaArtwork。
- AgentComposer、ToolCallCard、ApprovalCard、MemoryStatus。

业务组件只能使用 Design Tokens 和基础组件，不在内部创建独立视觉规范。组件库需要独立展示与交互测试页面，覆盖全部状态和边界尺寸。

## 6. API First 开发规则

每个接口必须按以下顺序进入开发（每个 API 先建立专用测试脚本，多次调用覆盖不同参数与边界分支，确定返回数据格式）：

1. 在隔离测试环境发起真实请求。
2. 保存脱敏响应样本。
3. 分析必填、可空、条件出现、分页和错误字段。
4. 覆盖未登录、无权限、空数据、限流和异常响应。
5. 定义请求与响应 Schema。
6. 转换为内部统一实体。
7. 加入缓存、字段池和请求编排。
8. 封装业务 Service 和 Agent Tool。
9. 编写契约测试。
10. 最后接入 UI。

未经字段分析的 API 不直接进入正式业务代码。

## 7. 全局字段池与请求编排

系统需要建立标准实体：

- `User`
- `Song`
- `Artist`
- `Album`
- `Playlist`
- `Comment`
- `PlaybackState`

多个 API 返回同一实体时，通过稳定 ID 合并字段，而不是重复维护。例如较早请求已经获得用户名，后续接口中的同名字段可以忽略，除非后续响应具有更高新鲜度或权威级别。

字段合并规则已确认：权威（账户级接口优先于公共接口）+ 新鲜度（同源取新）+ 完整度（完整字段优先，可空不覆盖非空）组合，配置化可调。字段合并至少考虑：

- 数据来源优先级。
- 请求时间和字段新鲜度。
- 字段是否完整。
- 是否允许空值覆盖。
- 登录账户数据和公共数据的权威差异。

已确认：用户数据（歌单/收藏）缓存 TTL 24 小时并在启动后后台刷新；首页推荐 30 分钟；搜索建议 1 小时；离线时展示缓存内容并明确标注；失败时 Section 级重试与降级；首页顶部菜单栏提供手动刷新按钮。应用打开后的具体请求顺序仍待确认。

## 8. 暂定系统架构

```text
Renderer/UI
  ├─ 播放器与业务页面
  ├─ Agent 侧边栏
  ├─ Tool Call 卡片
  ├─ Approval 卡片
  └─ Voice Overlay
          │
       Preload Bridge
          │
Electron Main / Local Agent Service
  ├─ Handwritten Agent Runtime
  ├─ Prompt & Context Builder
  ├─ Tool Registry
  ├─ Entity Resolver
  ├─ Deterministic Policy Engine
  ├─ Approval Coordinator
  ├─ Music API Adapters
  ├─ MCP Client Manager
  ├─ Dynamic Skill Loader
  ├─ Shell Executor
  ├─ Profile & Memory Store
  └─ Audit/Event Store
```

安全边界原则：Renderer 不直接获得 Node.js、Shell、文件系统、网易云凭据或任意网络访问能力；所有特权操作通过最小化桥接接口进入主进程或独立本地服务。

## 9. 非功能需求

### 9.1 安全

- 权限判断确定且可测试。
- 所有工具调用具备审计记录。
- Dynamic Skill 与 Shell 不在 Renderer 运行。
- 凭据不得进入模型上下文、日志或工具展示卡片。
- 拒绝操作必须保证底层副作用未发生。
- 高风险权限切换应有明确视觉反馈。

### 9.2 可靠性

- Agent 工具支持超时、取消和失败恢复。
- 审批等待不阻塞其他会话或播放器运行。
- API 局部失败不导致整个画像构建失败。
- Skill 或 MCP Server 崩溃不能拖垮主播放器。

### 9.3 性能

- 播放控制不依赖模型返回。
- Agent 页面加载不应被完整画像计算长期阻塞。
- 画像生成支持阶段性结果和后台增量更新。
- Prompt 仅加载当前任务需要的 Skill 与记忆。

### 9.4 隐私

- 明确哪些音乐数据发送给模型服务。
- 用户能够查看、导出和删除本地记忆。
- 日志、画像、语音和对话的保留策略需要单独确认。

## 10. 当前主要风险

| 编号 | 风险 | 当前处理方向 |
| --- | --- | --- |
| R-001 | 开源项目使用网易云非公开 API 的稳定性、登录风控、合规性和后续维护风险。 | 明确非官方属性，不提供公共代理服务；加入接口契约测试、版本锁定和失效降级。 |
| R-002 | Dynamic Skill JavaScript 获得宿主进程权限。 | 隔离执行，只允许通过受控能力桥调用。 |
| R-003 | Universal API Gateway 成为权限绕过入口。 | 路径清单、动作分类、Schema 和统一策略拦截。 |
| R-004 | Shell 高权限造成文件、隐私或系统损害。 | 独立安全等级、作用域限制、审计和审批。 |
| R-005 | 全量画像分析耗时长、API 请求量大。 | 缓存、增量分析、后台任务和阶段性画像。 |
| R-006 | 模型猜测或混淆歌曲/歌单 ID。 | Entity Resolver、候选集和消歧流程。 |
| R-007 | 全局快捷键冲突或无法可靠监听松开。 | 技术验证、允许改键、原生监听备选。 |
| R-008 | SSE、Electron IPC 与本地服务边界不清。 | 先确认运行拓扑，再冻结事件协议。 |
| R-009 | “性格分析”可能给出过度确定的心理结论。 | 定位为音乐偏好倾向，展示依据和不确定性。 |

## 11. 需求访谈与决策清单

问题按依赖顺序进行。每一轮确认后，将答案写入“决策记录”，并更新相应需求与验收标准。

需求访谈已完成（2026-08-03），P0/P1/P2 全部问题均有明确结论，见下方标记与第 12 节决策记录。

### P0：产品与系统边界

- D-001（已确认）：NcxMusic 是独立的高颜值 Agent 音乐播放器，不是官方客户端插件或增强层。
- D-002（已确认）：以开源项目形式开发和发布。
- D-003（部分确认）：使用 NeteaseCloudMusicApiEnhanced；通过官方网页登录捕获 `MUSIC_U`，不在应用中直接收集密码。API 的嵌入/服务运行方式和账户风险提示仍待确认。
- D-004（已确认）：Windows 与 macOS 双平台正式首发，Linux 以预览形式提供，不承诺支持等级。
- D-005（已确认）：侧栏采用上中下三区；主导航为“发现、搜索、小 N”，次导航覆盖我喜欢、自建和收藏歌单并独立滚动，底部账户和设置分两行展示。
- D-006（已确认）：用户配置自己的模型 API；首版协议为 OpenAI Compatible、Anthropic Messages 和 Gemini generateContent。
- D-007（部分确认）：NeteaseCloudMusicApiEnhanced 以内置本地依赖运行，不使用外部托管 API。Agent Runtime 与音乐 API 的最终进程隔离方式待确认。
- D-008（部分确认）：网易云登录位于模型配置前且允许跳过；跳过后调用游客登录。游客后的二次登录入口位置待确认。

### P1：Agent 行为

- D-101（部分确认）：首页通过“小 N 为你推荐”Section 主动展示推荐；是否允许自动续播或主动执行仍待确认。
- D-102（已确认）：一次任务最多 8 轮工具调用，单轮 60 秒，整体 10 分钟；超限时暂停并向用户说明。
- D-103（已确认）：只读操作自动重试 1 次；写操作禁止自动重试，失败直接如实告知用户。
- D-104（已确认）：高置信度直接执行；低置信度弹出候选卡片让用户选择，禁止静默猜测。
- D-105（已确认）：只支持一个连续会话；消息按 10 分钟无用户输入分块写入 SQLite，每块摘要并支持向量检索，Working Memory 决定进入 Prompt 的内容。
- D-106：Agent 的人格、称呼、语言和回答长度是否可配置？
- D-107（已确认）：用户播放操作即时生效；小 N 操作使用轻量提示并记录结构化 Action Journal，不弹操作冲突框。

### P1：安全与审批

- D-201（已确认）：M3 自动允许收藏/取消收藏、创建/删除歌单、歌单内添加/移除歌曲；发布/删除评论、签到、关注等社交写入仍需审批。
- D-202（已确认）：M4 默认关闭；开启后仅头像、昵称等展示资料自动执行，手机、邮箱等认证资料任何等级都需审批。
- D-203（已确认）：命令白名单 + 危险参数模式判定只读；工作目录默认应用数据目录下专用工作区；纯函数判断，LLM 不参与。
- D-204（已确认）：支持三档；持久化豁免写入用户可管理、可撤销的策略表，升级时重新审批。
- D-205（已确认）：关闭=拒绝；超时（默认 5 分钟，可配置）=拒绝；应用退出=任务保留，恢复时重新校验。
- D-206（已确认）：设硬禁清单（Credential Vault 访问、权限策略修改、未声明工作目录的命令等），任何等级与用户策略不可覆盖。

### P1：画像与记忆

- D-301（已确认）：允许完整性格分析；任何结论必须附证据摘要与不确定性说明，措辞限定为音乐偏好倾向分析而非诊断。
- D-302（已确认）：产品固定默认权重，设置页提供高级调整。
- D-303（已确认）：启动时检查；喜欢的音乐与自建歌单的加权累计变化（喜欢的音乐每首权重 1.5，自建歌单每首权重 1）超过 30 时触发增量更新。
- D-304（已确认）：支持查看详情、纠正单条偏好、删除全部画像、暂停更新。
- D-305（已确认）：默认明文存储，设置页可开启本地加密。
- D-306（已确认）：允许发送完整歌单；发送前在初始化卡片告知数据范围与费用影响，设置页提供“仅发送聚合结果”开关。

### P1：语音

- D-401（已确认）：ASR 本地优先（系统语音识别/本地 Whisper）；云 ASR 可选，费用由用户自带 Key 承担。
- D-402（已确认）：松手后浮窗直接关闭；执行完成后通过 Windows/macOS 原生通知 API 推送结果摘要。
- D-403（已确认）：首版不做 TTS 播报。
- D-404（已确认）：允许修改。
- D-405（已确认）：不落盘，流式处理即弃。

### P2：Skill 与 MCP

- D-501（已确认）：用户手写 + 本地导入；Git 仓库安装与应用内市场列入后续版本。
- D-502（已确认）：安装时按 SKILL.md 声明一次性同意；每次执行仍经过统一权限中间件与硬禁清单。
- D-503（已确认）：支持纯 JS 依赖包，安装前展示依赖清单确认并锁定版本；原生模块依赖暂不支持。
- D-504（已确认）：新增 `mcp_manager` Agent Tool，允许小 N 自主安装/删除 MCP Server（高风险操作，必须审批）；凭据存 Credential Vault。
- D-505（已确认）：支持 stdio 与 Streamable HTTP。
- D-506（已确认）：内置工具优先；设置页展示冲突清单并允许重命名外部工具。

### P2：API、数据与缓存

- D-601（已确认）：按首版页面反推：登录/用户、搜索、歌单 CRUD、歌曲详情/URL/歌词、专辑、歌手、推荐、评论、签到、播放排行。
- D-602（已确认）：为每个 API 创建专用测试 JS，多次调用覆盖不同参数与边界，确定返回数据格式。
- D-603（已确认）：权威 + 新鲜度 + 完整度组合规则，配置化可调。
- D-604（已确认）：用户数据 24h + 启动后台刷新；首页推荐 30min；搜索建议 1h；离线展示缓存并标注；Section 级重试与降级；首页顶部菜单栏提供手动刷新按钮。
- D-605（已确认）：全部存入 Credential Vault，由 API Adapter 注入，不落明文。

### P2：界面与体验

- D-701（已确认）：macOS 原生质感，浅色优先，参考 WWDC 2026 设计语言；品牌色、图标、字体待定稿。
- D-702（已确认）：默认 320px，可调 260~480px，可收起为窄条。
- D-703（已确认）：状态、参数摘要、耗时、结果摘要 + 可展开技术详情。
- D-704（已确认）：保留 Windows 原生窗口能力，Snap Layout 跟随系统。
- D-705（已确认）：全屏与沉浸为两个独立状态，最大化保留系统行为。
- D-706（已确认）：中文首版；无障碍基础（语义树、键盘完整操作、对比度达标）；多语言架构预留，首版不做 i18n。
- D-707（已确认）：页面使用 Section 化结构，并建设统一的 Design Tokens、基础组件、浮层反馈和音乐业务组件。

## 12. 决策记录

| 日期 | 决策编号 | 结论 | 影响范围 |
| --- | --- | --- | --- |
| 2026-08-03 | - | 建立 Draft 0.1，开始结构化需求访谈。 | 全局 |
| 2026-08-03 | D-001 | 建设独立的高颜值、完整 AI Agent 音乐播放器。 | 产品定位、页面范围、设计系统 |
| 2026-08-03 | D-002 | NcxMusic 作为开源项目开发和发布。 | 发布、合规、安全、贡献流程 |
| 2026-08-03 | D-003 | 使用 NeteaseCloudMusicApiEnhanced；打开网易云官方网页登录并捕获 `MUSIC_U`。 | 登录、API、凭据安全、账户隔离 |
| 2026-08-03 | D-005 | 确认首版页面初始范围；Agent 暂定名“小 N”；一级导航待确认。 | 信息架构、路由、首版范围 |
| 2026-08-03 | D-006 | 用户自备 API Key；支持厂商预设、自定义 OpenAI 兼容服务和跳过配置。 | 首次引导、模型设置、凭据安全 |
| 2026-08-03 | D-007 | 音乐 API 作为应用内本地依赖运行。 | 进程架构、打包、升级和故障隔离 |
| 2026-08-03 | D-008 | 网易云登录排在模型配置之前且可跳过；跳过后自动建立游客会话。 | 首次引导、账户状态、游客能力 |
| 2026-08-03 | D-005 | 冻结 Codex 式三区侧栏、三类歌单次导航、折叠和滚动规则；游客完全隐藏次导航。 | 导航、布局、账户状态 |
| 2026-08-03 | D-006 | 冻结 OpenAI Compatible、Anthropic Messages、Gemini generateContent 三种首版协议。 | Provider Adapter、模型设置、测试 |
| 2026-08-03 | D-101 | 首页加入满足登录、模型和画像条件后出现的“小 N 为你推荐”Section。 | 首页、推荐、用户画像 |
| 2026-08-03 | D-105 | 小 N 使用单会话、10 分钟分块、SQLite 历史、块摘要、向量检索与 Working Memory。 | Agent Runtime、上下文、存储 |
| 2026-08-03 | D-107 | 播放操作使用结构化事件日志；小 N 操作通过轻量消息提示，不因用户同时操作弹冲突框。 | 播放器、Agent Tool、通用反馈组件 |
| 2026-08-03 | D-707 | 页面 Section 化，并建设覆盖基础控件、浮层、反馈、布局和音乐业务的通用组件体系。 | 前端架构、设计系统、测试 |
| 2026-08-03 | D-004 | Windows 与 macOS 双平台正式首发，Linux 预览。 | 平台、发布、里程碑 |
| 2026-08-03 | D-102~104 | 冻结工具轮数/超时、失败重试与同名消歧策略。 | Agent Runtime、工具层 |
| 2026-08-03 | D-201~206 | 冻结 M3/M4 边界、Shell 白名单与工作区、审批三档与超时行为、硬禁清单。 | 权限系统、审批流程 |
| 2026-08-03 | D-301~306 | 冻结画像表达深度、权重、更新触发、用户控制权、加密与数据出境边界。 | 用户画像、隐私 |
| 2026-08-03 | D-401~405 | 冻结语音方案：本地优先 ASR、浮窗直接关闭+原生通知、无 TTS、音频不落盘。 | 语音、隐私 |
| 2026-08-03 | D-501~506 | 冻结 Skill 来源/授权/依赖、MCP 传输、mcp_manager 工具与命名冲突规则。 | Skill、MCP、安全 |
| 2026-08-03 | D-601~605 | 冻结首批 API 清单、探测工具、字段合并规则、缓存策略与凭据保存。 | API First、数据层 |
| 2026-08-03 | D-701~706 | 冻结视觉方向、侧栏尺寸、全屏沉浸状态、无障碍与多语言要求。 | 设计系统、窗口、无障碍 |

## 13. 里程碑（需求访谈完成，待排期）

暂定拆分方向，不代表已确认开发顺序：

1. 技术验证：登录/API、播放链路、模型 Tool Call、权限挂起、快捷键。
2. 基础播放器与统一数据模型。
3. 手写 Agent Runtime 与音乐工具。
4. 用户画像、记忆与推荐闭环。
5. 双权限按钮、执行卡片与审批卡片。
6. 语音浮窗与 ASR（首版无 TTS）。
7. Dynamic Skill 与 MCP 管理。
8. 双平台视觉适配、稳定性和发布。

## 14. 完成定义

本需求阶段完成需满足：

- P0、P1、P2 问题全部有明确结论（已完成，见第 11 节）。
- MVP 和后续版本边界清晰。
- 每项核心功能具备可测试的验收标准。
- 权限矩阵不存在模糊动作。
- 数据、进程、信任和网络边界明确。
- API 清单及测试优先级确定。
- 关键技术风险已有验证任务和退出条件。
